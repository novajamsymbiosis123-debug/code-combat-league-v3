{
  "name": "code-combat",
  "version": "2.0.0",
  "description": "Real-time competitive coding platform with deterministic server-side evaluation",
  "main": "server.js",
  "license": "MIT",
  "engines": {
    "node": ">=18.0.0",
    "npm": ">=9.0.0"
  },
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "health": "curl -sf http://localhost:${PORT:-3000}/health || echo FAIL"
  },
  "dependencies": {
    "express": "^4.18.2",
    "socket.io": "^4.7.4"
  },
  "devDependencies": {
    "nodemon": "^3.0.2"
  }
}
